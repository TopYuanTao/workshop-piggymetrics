mkdir -p /code/current; cd /code/current; git clone http://root:alauda1234@${GITLAB_ADDR}/root/${GITLAB_PROJECT}.git; git config --global user.email "topyuan@alauda.com"; git config --global user.name "TopYuan"; rm -fR ${GITLAB_PROJECT}/*; \cp -fR /code/unzip/${GITLAB_PROJECT}/* ${GITLAB_PROJECT}/; cd ${GITLAB_PROJECT}; git add *; git commit -m "Update at $(date +%Y%m%d-%H%M%S)" -a; git remote rm origin; git remote add origin http://root:alauda1234@${GITLAB_ADDR}/root/${GITLAB_PROJECT}.git; git push origin master; cd /code/current; git clone http://root:alauda1234@${GITLAB_ADDR}/root/${GITLAB_PROJECT}-config.git; rm -fR ${GITLAB_PROJECT}-config/*; \cp -fR /code/unzip/${GITLAB_PROJECT}-config/* ${GITLAB_PROJECT}-config/; cd ${GITLAB_PROJECT}-config; git add *; git commit -m "Update at $(date +%Y%m%d-%H%M%S)" -a; git remote rm origin; git remote add origin http://root:alauda1234@${GITLAB_ADDR}/root/${GITLAB_PROJECT}-config.git; git push origin master
